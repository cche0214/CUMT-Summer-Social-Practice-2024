[build]
  # 指定你要部署的目录
  publish = "docs"
  # 指定无服务器函数的目录
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
